# CRM-123 — placeholder

This file exists only to give the `CRM-123` branch a commit to open a pull request
from, so the `/pr` command can be rehearsed end to end.

It is not part of the app: nothing under `force-app` is touched, so a merge of this
branch deploys no metadata to the `ai-poc` dev org.

Safe to delete once the dry run is finished.
